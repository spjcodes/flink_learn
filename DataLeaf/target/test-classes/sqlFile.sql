-- create table ta
create table ta                      (
    id varchar(32) not null primary key
    , name varch ar(32)
, age       int(3) default 0
    -- gender char(1)
            );

-- create table ta
create table ta                      (
                                         id varchar(32) not null primary key
    , name varch ar(32)
    , age       int(3) default 0
    -- gender char(1)
);

-- create table ta
create table ta                      (
                                         id varchar(32) not null primary key
    , name varch ar(32)
    , age       int(3) default 0
    -- gender char(1)
);
-- create table ta
create table ta                      (
                                         id varchar(32) not null primary key
    , name varch ar(32)
    , age       int(3) default 0
    -- gender char(1)
)